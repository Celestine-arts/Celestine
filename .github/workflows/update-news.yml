name: Update News

on:
  schedule:
    - cron: "0 6 * * *"   # daily at 06:00 UTC — adjust as you like
  workflow_dispatch: {}    # lets you trigger it manually from the Actions tab

jobs:
  update-news:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install rss-parser

      - name: Run news update script
        run: node scripts/update-news.mjs

      - name: Open pull request with changes
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "Auto: update news from RSS sources"
          title: "News update — new items for review"
          body: "Automated run pulled new items from tracked RSS sources. Review each card before merging."
          branch: auto/news-update
          delete-branch: true
